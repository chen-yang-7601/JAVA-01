前言：
  1. 测试程序说明：
     通过对一个java程序GCLogAnalysis，分析其在不同的GC策略下的运行情况。这个程序在设定的时间内（默认为一秒），随机地创建100K以内的(byte/int/double类型）的随机对象，执行效率以时间范围内生成的对象数量来衡量。
  2. 环境说明：
     机器1： 16G内存，i5CPU, WIN10
     机器2： 16G内存，i7-1165G7cpu, WIN10
 
 ParallelGC:
 
 
